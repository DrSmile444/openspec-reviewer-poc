## Why

The repo has no README. Anyone opening it cannot tell what it is for. This change is
also the vehicle for a PoC: it is deliberately trivial so the surrounding workflow
(branch → implement → review → commit) can be exercised end to end.

## What Changes

Add a `README.md` at the repo root describing the purpose of this PoC repo and how
the openspec workflow is wired here.

## Capabilities

### New Capabilities
- `project-readme`: the repo exposes a root README describing its purpose and workflow

### Modified Capabilities

## Impact

- New file: `README.md`
- No code, no dependencies, no APIs affected
